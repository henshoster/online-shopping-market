import { CategoryIdToNamePipe } from './category-id-to-name.pipe';

describe('CategoryIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
